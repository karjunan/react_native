import {Text, View} from 'react-native';
const ComponentExample = props => {
  return (
    <View>
      <Text> Hello there this is working now {props.name}</Text>
    </View>
  );
};
export default ComponentExample;
